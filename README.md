# RAG Backend

Backend API Gateway for the RAG system.  
Handles ETL, Supabase integration, and routes for embeddings.

## Setup

1. Clone the repo:
   ```bash
   git clone git@github.com:Nwodothomas/rag-backend.git
   cd rag-backend
